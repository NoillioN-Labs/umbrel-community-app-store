#!/usr/bin/env bash
set -euo pipefail

: "${APP_DATA_DIR:?Umbrel must supply APP_DATA_DIR}"

for directory in suite node node-logs; do
  install -d -m 0750 -o 1000 -g 1000 "${APP_DATA_DIR}/data/${directory}"
done
